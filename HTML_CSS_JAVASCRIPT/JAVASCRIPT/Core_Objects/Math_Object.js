/*
Math objects lets you perform mathematical tasks;
they are:

Math.E Euler constant;
Math.LN2 Natural log of the value 2;
Math.LN10 Natural log of the value 10;
Math.LOG2E The base 2 log of Euler constant;
Math.LOG10E The base 10 log of Euler constant;
Math.PI Return the constant PI;

Math.abs(x) returns the absolute value of x;
Math.acos(x) returns the arccosine(cosseno) of x in radians;
Math.asin(x) returns the arcsine(seno) of x in radians;
Math.atan(x) returns the arctangent of x as a numeric value between -PI/2 and PI/2 radians;
Math.atan2(y,x) returns the arctangent of the quotient(quociente) of it's arguments;
Math.ceil(x) returns x rounded upwards to the nearest integer;
Math.cos(x) returns the cosine of x in radians;
Math.exp(x) returns the value of E^x (expoente);
Math.floor(x) returns x rounded downwards to the nearest integer;
Math.log(x) returns the natural logarithm (base E) of x;
Math.max(x,y,z,...,n) returns the highest value;
Math.min(x,y,z,...,n) returns the lowest value;
Math.pow(x,y) returns the value of x to the power of y (potência);
Math.random() returns a random number between 0 and 1;
Math.round(x) rounds x to the nearest integer;
Math.sin(x) returns the sine of x in radians;
Math.sqrt(x) returns the square root of x;
Math.tan(x) returns the tangent of an angle;
*/

console.log(Math.PI);
console.log(Math.round(41.4));
console.log(Math.round(41.5));
console.log(Math.sqrt(81));