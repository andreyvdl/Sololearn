/*
You can use multiple parameters on the same function;
*/

function divide(a, b){
	var c = a / b;
	if (b == 0) {
		console.log("LOL, NO");
	}
	else {
		console.log(c);
	}
}

divide(69, 3);
divide(2, 0);
divide(10,);