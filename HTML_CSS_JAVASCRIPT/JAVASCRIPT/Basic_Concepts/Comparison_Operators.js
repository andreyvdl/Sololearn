/*
Comparison operators are used to compare:
"==" checks if two conditions are equal;
"===" checks if two conditions are IDENTICAL;
"!=" checks if two conditions are not equal;
"!==" checks if two conditions are not IDENTICAL;
">" checks if left condition is bigger than the right;
">=" checks if left condition is bigger or equal to the right;
"<" checks if left condition is smaller to the right;
"<=" checks if left condition is smaller or equal to the right;
IDENTICAL != equal;
*/

var num = 10;
console.log(num == 8);