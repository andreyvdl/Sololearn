#include <stdio.h> //It's the library that contains "printf()" function.

int main() { //It's the entry of the program, the "{}" indicates where the block starts and end.
	printf("Hello, World!\n"); //The "\n" indicates a break line.
	return 0; //"return" ends the process of "main", it should return 0 if the program runs ok, and any other number if failed.
}
