#include <stdio.h>

int	main() {
	int	score = 89;

	if (score >= 90)
		printf("Top 10%%.\n");
	else //The else statement say that: "if the if statement is false, then execute this".
		printf("Less than 90.\n");

	return 0;
}
