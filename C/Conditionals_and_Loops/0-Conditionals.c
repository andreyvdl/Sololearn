#include <stdio.h>

int	main() {
	int	score = 89;

	if (score > 75) //The "if" will check if the expression is true, if it is then it will enter to execute the statements.
		printf("You passed.\n");

	return 0;
}
