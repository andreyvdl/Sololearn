#include <stdio.h>

int	main() {
	int i;
	int max = 10;

	for (i = 0; i < max; i++){
		printf("%d\n", i);
	}
	return 0;
}

/*
 * The "for" works like "while" it will create a loop, but you define the start value of the increment and the condition to continue the loop, on the same line.
 */ 
